import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    achievementCard: {
        width: '48%',
        alignItems: 'center',
        padding: 16,
        borderRadius: 12,
        borderWidth: 1,
    },
    achievementIcon: {
        fontSize: 24,
        marginBottom: 8,
    },
    achievementTitle: {
        fontSize: 14,
        fontWeight: '600',
        textAlign: 'center',
        marginBottom: 4,
    },
    achievementDesc: {
        fontSize: 12,
        opacity: 0.8,
        textAlign: 'center',
    },
});